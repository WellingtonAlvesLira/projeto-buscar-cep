import Vue from 'vue';
import Router from 'vue-router';

//Importando os components para a criação de rotas da aplicação.



Vue.use(Router);

const routes = [
    // {path: '/', name: 'LoginPage', component: LoginPage},
    // {path: '/book', name:'BookListPage', component: BookListPage},
    // {path: '/book/:id', name:'BookEntrada', component: BookEntradaPage},
    // {path: '/biblioteca', name:'BibliotecaListPage', component: BibliotecaListPage},


]

export default new Router({
    routes,
});